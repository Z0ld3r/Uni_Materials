#include <stdio.h>
int main(){
	int array[10];
	int T[]= {1, 2, 3, 4, 5};
	
	printf("%d\n", sizeof(array));
	
	int sum = 0;
	
	for (int i=0; i<(sizeof(T)/sizeof(T[0])){
		sum += T[i];
	}
	printf("%d\n", sum);
}